# Gateway Protocol (Draft)

ResilientDNS uses a simple JSON batching protocol between the LAN resolver and the remote gateway.

This document will define:
- Request/response schema
- Versioning strategy
- Error handling rules
- Backward compatibility notes
